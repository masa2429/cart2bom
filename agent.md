# Cart2BOM

複数通販サイトのカートを、保存・共有可能な部品リストへ変換するUserScriptを開発する。初期版は秋月電子通商に対応し、Tampermonkey／Violentmonkey上で動作させる。

## プロジェクト構造

- `cart2bom_specification.md` — 現在の正式な仕様書。実装判断と実装順序の基準
- `CLAUDE.template.md` — この指示書の作成元テンプレート。製品仕様ではない
- `akizuki_cart_bookmarklet.html` — 秋月用の既存試作版。挙動の参考にとどめ、コードを不用意に変更・コピーしない
- `src/` — UserScript本体。共通ロジック、サイト別アダプター、ストレージ、UIを配置する
- `tests/` — 単体テストと保存済みHTML fixtureを配置する
- `scripts/` — ビルド補助スクリプトを配置する
- `dist/cart2bom.user.js` — 配布用の生成物
- `docs/` — READMEから参照する補足資料。仕様書を移動する場合は履歴と参照先を保つ

## 重要ルール

- 実装前にリポジトリ全体と`cart2bom_specification.md`を確認し、既存ファイルを不用意に削除・上書きしない
- 仕様書の「26．実装順序」に従い、Step 1からStep 8まで順番に進める。各Stepのビルドとテストが成功するまで次へ進まない
- 本仕様と既存コードが矛盾する場合は本仕様を優先し、変更理由をREADMEまたは作業報告へ記録する
- AkiBoostは操作感だけを参考にし、ソースコードをコピーしない。第三者コードは利用前にライセンスを確認する
- `akizuki_cart_bookmarklet.html`は試作・検証資料として保持し、UserScriptの主実装にはしない
- 外部サーバーへの商品情報送信、注文確定、決済画面解析、Cookie・セッション・ログイン情報の取得を実装しない
- 対応サイト固有のDOMセレクタは各アダプター内へ閉じ込め、共通UIや共通ロジックから参照しない
- 実サイトDOMを確認できない箇所に大量の推測フォールバックを追加しない。fixtureと警告を使って検証可能な構造にする
- 生成物以外のコードは`dist/`へ置かず、`dist/cart2bom.user.js`を直接編集しない

## 技術スタック（厳守）

- TypeScript（strictモード）／HTML／CSS
- Node.jsとnpm。依存バージョンは`package-lock.json`で固定する
- TypeScriptを単一のUserScriptへバンドルし、`dist/cart2bom.user.js`を生成する
- DOMテストは保存済みHTML fixture、ストレージテストはメモリ実装を使用する
- UserScript APIへの依存は`StorageProvider`などの境界へ隔離し、共通ロジックをブラウザ拡張へ移植可能にする
- `any`は原則禁止。DOMの`null`、不正入力、未知のスキーマバージョンを明示的に扱う
- 金額は整数の円、数量は正の整数、日時はISO 8601文字列として扱う

## ビルドと実行

- 依存関係の導入: `npm install`
- ビルド: `npm run build`
- テスト: `npm test`
- Step完了時は必ずビルドとテストを実行し、成功・失敗と未検証事項を報告する
- Step 1では、生成した`dist/cart2bom.user.js`をTampermonkeyへインストール可能な状態にする
- 実サイト確認が必要な場合は、コードを書く前に利用可能なカートHTML fixtureまたは手動確認環境の有無を確認する
- 「ビルド成功」は動作確認完了ではない。自動テストと、可能な範囲の実動作確認を分けて記録する

## 実装順序

1. プロジェクト初期化：TypeScript、ビルド、UserScriptヘッダー、最小固定ボタン
2. データモデルとストレージ：モデル、Provider、バリデーション、単体テスト
3. 秋月アダプター：ページ判定、抽出、警告、fixtureテスト
4. カート確認画面：読み取り、編集、削除、検証、保存
5. 保存済みリスト：一覧、開く、複製、名前変更、削除
6. エクスポート：CSV、TSV、JSON、Markdown、一括注文、コピー、保存
7. インポート：JSONファイル／テキスト、スキーマ検証、重複確認
8. 仕上げ：設定、通知、エラー、README、LICENSE、手動テスト、リリースビルド

## 実装ルール

- 1機能ごとに、実装、単体テスト、ビルド、結果確認までを完了させる
- 公開関数と主要な判定処理へ、理由が分かる簡潔なコメントを書く。複雑なセレクタには対象要素の説明を付ける
- UI生成、DOM解析、文字列生成、ファイル保存を分離する
- ユーザー入力を`innerHTML`へ直接代入しない。可能な限りDOM APIと`textContent`を使用する
- 取得できない任意項目は`null`または空文字とし、必須項目を取得できる商品まで破棄しない
- 部分的な抽出失敗は、取得済み商品と利用者向け警告を返す。詳細は`[Cart2BOM]`接頭辞の開発ログへ分離する
- クリップボード書き込み、ファイル保存、ページ遷移は利用者の明示操作時だけ行う
- 破損した保存データを勝手に削除しない。可能ならバックアップ用JSONを出力できる状態を保つ
- 各Step終了時に、実装済み機能、未実装項目、既知の制限、テスト結果、実サイト確認が必要な箇所をまとめる

## Git

- 各Step、独立した機能、バグ修正、文書更新の検証が完了した時点で、Conventional Commits形式により意味のある最小単位ごとに自律的にコミットする
- ビルドまたはテストが失敗した状態は原則コミットせず、修正と再検証を終えてからコミットする
- 形式: `type(scope): 日本語の説明`
- すべてのコミットはユーザー単独の著作として扱い、コミットメッセージへ`Co-authored-by`など共同著者を示すトレーラーを追加しない
- コミット時はリポジトリに設定されたユーザーの氏名とメールアドレスを著者・コミッターとして使用し、CodexやAIエージェントの名義へ変更しない
- プロジェクト初期化、モデル／ストレージ、各アダプター、各エクスポーター、UI機能、バグ修正を混在させない
- ユーザーの未コミット変更を保持し、無関係な変更をコミットへ含めない

## ミスの記録

<!-- 失敗するたびに追記する。書式：YYYY-MM-DD ／ 症状 ／ 原因 ／ 対策 -->
- 2026-08-05 ／ カート編集画面の初回合計が0円になりUIテストが失敗 ／ モーダル構築中の行に`isConnected`を使い、DOM追加前の有効行まで除外した ／ 行削除の判定を`parentElement`の有無へ変更し、初期表示と削除後の双方をテストする
- 2026-08-05 ／ 実カート12商品で無関係な6件の警告を表示 ／ ページ全体の商品リンクを走査し、「最近チェックした商品」まで数量欠落として扱った ／ カート商品行へ走査範囲を限定し、警告の対象と理由を画面へ表示する回帰テストを追加する
